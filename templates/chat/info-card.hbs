<div class="crawl-card">
  <header class="crawl-card-head">
    <span class="crawl-tab">{{actorName}}</span>
    <h4>{{label}}</h4>
  </header>

  {{#if flavor}}<div class="crawl-card-flavor">{{{flavor}}}</div>{{else}}<div class="crawl-card-detail">Passive — no description yet.</div>{{/if}}
</div>
